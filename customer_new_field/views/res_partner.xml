<odoo>
    <record id="view_partner_form_inherit_threshold" model="ir.ui.view">
        <field name="name">res.partner.form.inherit.threshold</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <field name="category_id" position="after">
                <field name="restricted"/>
                <field name="count" invisible="restricted == False"/>
            </field>
        </field>
    </record>
</odoo>